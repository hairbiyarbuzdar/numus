# Nginx reverse proxy for Numu.
#
# Routes:
#   /api/       -> Express backend  (port 4000)
#   /socket.io/ -> Socket.io        (port 4000, WebSocket upgrade)
#   /health     -> backend health check
#   /           -> Next.js frontend (port 3000)
#
# INSTALL (domain numu.com.pk already filled in):
#   1. sudo cp deploy/nginx-numu.conf /etc/nginx/sites-available/numu
#   2. sudo ln -sf /etc/nginx/sites-available/numu /etc/nginx/sites-enabled/numu
#   3. sudo rm -f /etc/nginx/sites-enabled/default
#   4. sudo nginx -t && sudo systemctl reload nginx
#   5. sudo certbot --nginx -d numu.com.pk -d www.numu.com.pk   # adds HTTPS (rewrites this file)

server {
    listen 80;
    listen [::]:80;
    server_name numu.com.pk www.numu.com.pk;

    # Allow large-ish uploads (backend accepts 10mb JSON bodies)
    client_max_body_size 12M;

    # ─── Backend API ──────────────────────────────────────────────────────────
    location /api/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ─── Socket.io (WebSocket) ────────────────────────────────────────────────
    location /socket.io/ {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade    $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host       $host;
        proxy_set_header X-Real-IP  $remote_addr;
        proxy_read_timeout 86400;
    }

    # ─── Health check ─────────────────────────────────────────────────────────
    location /health {
        proxy_pass http://127.0.0.1:4000;
        proxy_set_header Host $host;
    }

    # ─── Next.js frontend (everything else) ───────────────────────────────────
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
